<%= form_with(model: item, local: true) do |form| %>
  <% if item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(item.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
      <% item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :asin %>
    <%= form.text_field :asin, id: :item_asin %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_area :title, id: :item_title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :item_description %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.number_field :price, id: :item_price %>
  </div>

  <div class="field">
    <%= form.label :amazon_url %>
    <%= form.text_area :amazon_url, id: :item_amazon_url %>
  </div>

  <div class="field">
    <%= form.label :brand %>
    <%= form.text_field :brand, id: :item_brand %>
  </div>

  <div class="field">
    <%= form.label :group %>
    <%= form.text_field :group, id: :item_group %>
  </div>

  <div class="field">
    <%= form.label :main_image %>
    <%= form.text_area :main_image, id: :item_main_image %>
  </div>

  <div class="field">
    <%= form.label :stocks %>
    <%= form.number_field :stocks, id: :item_stocks %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
